/** Keyboard-first skip link; becomes visible on focus and jumps to <main id="main">. */
export function SkipLink() {
  return (
    <a className="skip-link" href="#main">
      Skip to main content
    </a>
  );
}
